pushq %rbp      ; save callers frame ptr
movq %rsp, %rbp ; setup our frame pointer (rbp = rsp)
subq $32, %rsp  ; reserve 32 bytes for locals

after prologue 

  ┌─────────────┐
  │  arg2       │  ← rbp+24 (or wherever)
  │  arg1       │  ← rbp+16
  │  ret addr   │  ← rbp+8  (pushed by call)
  │  saved rbp  │  ← rbp    (pushed by our pushq)
  │  local 0    │  ← rbp-8
  │  local 1    │  ← rbp-16
  │  local 2    │  ← rbp-24
  │  local 3    │  ← rbp-32 ← rsp
  └─────────────┘

... body ...

access locals via rbp-N
access params via rbp+N

; write return value
movq %rax, 24(%rbp)

; epilogue
movq %rbp, %rsp ; tear down frame (undoes subq)
popq %rbp       ; restore caller frame ptr
retq

... at callsite ...








- moves into -rsp

- rsp (caller)

- ret addr (pushed by callq)

- xxx move dests

- xxx ...










