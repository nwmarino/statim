rib index;

$public
main :: () -> s64 {
    let a: bool = true;
    let b: bool = false;

    if a && b {
        a = !b;
    } else if a || b {
        b = !a;
    }

    // !a, !b => false
    // a, !b  => true
    // !a, b  => true
    // a, b   => true
    ret a ^ b; 
}
